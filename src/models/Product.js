const { Pool, Client } = require('pg');


// const text = 'INSERT INTO Product (title, description, url) VALUES($1, $2, $3) RETURNING *'
// const values = [
//     'React Native', 
//     'Build native apps with React',
//     'http://github.com/facebook/react-native']

const ProductSchema = new Pool({
    
});